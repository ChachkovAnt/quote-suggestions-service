user nobody nogroup;
worker_processes auto;

events {
  worker_connections 512;
}

http {
  server {
    listen *:80;

    location / {
        proxy_pass http://test_quotes_generate_quotes-suggestions-service_1:5000;
    }
  }
}
